# Animal tracking application

The application uses the following commands:
- list (to display the list of animals)
- add (to add an animal to the list)
- change (to modify its details)
- del (to delete the record with the corresponding ID)
- h (help)
